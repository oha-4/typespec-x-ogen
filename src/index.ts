export { $lib } from "./lib.js";

/**
 * Decorator implementations are exported here as `$<decoratorName>` so the
 * TypeSpec compiler can link them to the `extern dec` declarations in
 * `lib/main.tsp`. They will be added as the ogen extensions are implemented.
 */
